{
  "name": "langgraph-checkpoint-neo4j-python",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "uv run pytest --tb=short -q",
    "test": "uv run pytest",
    "test:cov": "uv run pytest --cov --cov-report=term:skip-covered",
    "lint": "uv run ruff check . && uv run ruff format --check .",
    "format": "uv run ruff check --fix --unsafe-fixes . && uv run ruff format .",
    "sync": "uv sync",
    "build": "uv build"
  }
}
